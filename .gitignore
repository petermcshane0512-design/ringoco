# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# A2P submission state (contains Twilio SIDs only, no secrets, but local-only)
.a2p-state.json

# vercel
.vercel
.vercel-env-check
.stripe-prices.json

# typescript
*.tsbuildinfo
next-env.d.ts

.vercel
.env*.local

# clerk configuration (can include secrets)
/.clerk/

# ── Local-only artifacts not for the public repo ──
# Temp JSON dumps from debug scripts (Twilio account info, Vapi transcripts)
.tmp-*.json
# Scraped/enriched business leads (real PII — local only)
/leads/
/.leads-tmp/
# LLC paperwork
/BELLAVEGOLLC
# Playwright MCP cache
/.playwright-mcp/
# Social engagement bot — saved Meta session cookies (full bearer tokens, NEVER commit)
/.auth/
# Page snapshots from local dev
/*-snapshot.md

.gmail-oauth-client.json
.env.prod-temp
