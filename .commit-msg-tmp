Conversion stack v2: hero zip lookup + exit intent + Loom placeholder + guarantee stamps

For $10M ARR by May 12 2027 — every email lead must close.

- HeroZipForm: zip-code microcommitment AS primary above-fold CTA
  (CXL: form-first heroes lift conversion 30-50% vs button-first;
  typing the zip = Cialdini consistency bias toward completing)
- TripleGuaranteeBadge: 3 visual dashed stamps near offer card
  (30-DAY refund, 1-JOB or free, KEEP all leads — scannable in 2 sec)
- FounderVideoCard: 16:10 Loom placeholder w/ click-to-play modal that
  routes to "text Peter" SMS fallback until video lands. Swap
  LOOM_EMBED_URL constant to go live.
- ExitIntentPopup: mouseleave-top-edge bounce recovery modal,
  24h cookie suppression, single CTA + phone fallback
  (CXL: recovers 10-15% of would-be bounces — critical because cold
  email landing = paid traffic = each bounce = wasted spend)
- Imports useEffect + useRef for exit-intent + modal state
- Removed stub HeroZipForm/TripleGuaranteeBadge/FounderVideoCard/
  ExitIntentPopup that other terminal added to keep build green

Replaces simple ctaHeroPrimary button + static founder bar with full
microcommitment + bounce-recovery + video-trust stack.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
